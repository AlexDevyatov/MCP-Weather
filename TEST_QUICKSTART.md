# ⚡ Быстрое тестирование MCP сервера

## 🎯 Проблема: "session_id is required"

Если вы получили ошибку `session_id is required` при тестировании через curl, это нормально! 

**Причина:** MCP через SSE транспорт требует установления SSE соединения для получения `session_id`. Прямые HTTP POST запросы к `/messages/` не работают без активного SSE соединения.

## ✅ Решение: Используйте Python скрипт

### Шаг 1: Установите зависимости

```bash
pip install mcp httpx
```

Или если используете виртуальное окружение:

```bash
source venv/bin/activate
pip install mcp httpx
```

### Шаг 2: Запустите тест

```bash
python test_mcp.py --port 9001
```

Скрипт автоматически:
- ✅ Проверит health check
- ✅ Установит SSE соединение через MCP SDK
- ✅ Получит список инструментов
- ✅ Протестирует все инструменты

## 🔍 Что проверит скрипт

1. **Health Check** - доступность сервера
2. **Список инструментов** - получение через SSE
3. **Текущая погода** - тест `get_current_weather`
4. **Прогноз погоды** - тест `get_weather_forecast`
5. **Поиск местоположения** - тест `search_location`

## 📋 Пример вывода

```
======================================================================
🧪 Тестирование MCP Weather сервера
======================================================================
HTTP URL: http://localhost:9001
SSE URL: http://localhost:9001/sse
======================================================================

🔍 Тест 1: Health Check
   GET http://localhost:9001/health
   ✅ Успешно: OK

🔍 Тест 2: Список инструментов (через SSE)
   Подключение к http://localhost:9001/sse
   ✅ Найдено инструментов: 3
      • get_current_weather: Получение текущей погоды...
      • get_weather_forecast: Получение прогноза погоды...
      • search_location: Поиск координат по названию города

🔍 Тест 3: Получение текущей погоды (Москва)
   Подключение к http://localhost:9001/sse
   ✅ Успешно получена погода:
   ────────────────────────────────────────────────────────────
   🌤️ Погода в Москве
   ...
```

## 🐛 Если что-то не работает

### Ошибка "MCP SDK не установлен"

```bash
pip install mcp httpx
```

### Ошибка подключения

1. Проверьте, что контейнер запущен:
   ```bash
   docker ps | grep mcp-weather
   ```

2. Проверьте порт:
   ```bash
   docker port mcp-weather
   ```

3. Проверьте логи:
   ```bash
   docker logs mcp-weather
   ```

### Health check не проходит

```bash
curl http://localhost:9001/health
```

Должно вернуть: `OK`

## 📚 Подробная документация

Полная документация по тестированию: [TEST_MCP.md](TEST_MCP.md)

---

**Готово! Теперь вы можете протестировать MCP сервер правильно! 🎉**
