# 🔧 Настройка окружения

## Проверка версии Python

MCP SDK требует **Python 3.10 или выше**. Проверьте вашу версию:

```bash
python3 --version
```

Если версия ниже 3.10, вам нужно обновить Python.

## Обновление Python

### macOS

1. **Через Homebrew (рекомендуется):**
   ```bash
   brew install python@3.11
   # или
   brew install python@3.12
   ```

2. **Скачать с python.org:**
   - Перейдите на [python.org/downloads](https://www.python.org/downloads/)
   - Скачайте и установите Python 3.10+

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install python3.10 python3.10-venv
# или для более новой версии
sudo apt install python3.11 python3.11-venv
```

### Windows

1. Скачайте установщик с [python.org/downloads](https://www.python.org/downloads/)
2. Установите Python 3.10 или выше
3. Убедитесь, что добавили Python в PATH

## Создание виртуального окружения с правильной версией

После установки Python 3.10+:

```bash
# Если у вас несколько версий Python, используйте конкретную версию:
python3.10 -m venv venv
# или
python3.11 -m venv venv
# или
python3.12 -m venv venv

# Активация
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

## Проверка установки

После установки проверьте:

```bash
python --version  # Должно быть 3.10+
pip list | grep mcp  # Должен показать установленный пакет mcp
```

## Решение проблем

### "python3.10: command not found"

Убедитесь, что Python 3.10+ установлен и доступен в PATH:

```bash
which python3.10
# или
which python3.11
```

Если команда не найдена, добавьте Python в PATH или используйте полный путь:

```bash
/usr/local/bin/python3.10 -m venv venv
```

### "No module named venv"

Установите пакет для создания виртуальных окружений:

```bash
# Ubuntu/Debian
sudo apt install python3.10-venv

# macOS (обычно уже установлено)
# Если нет, переустановите Python через Homebrew
```
