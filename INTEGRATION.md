# 🔌 Интеграция MCP Weather сервера

## ✅ Что уже сделано

1. ✅ Python 3.11 установлен через Homebrew
2. ✅ Виртуальное окружение создано с Python 3.11
3. ✅ Все зависимости установлены
4. ✅ Конфигурация для Cursor настроена в `.cursor/mcp.json`

## 🚀 Следующие шаги

### Для Cursor IDE (Workspace)

Конфигурация уже настроена! Просто:

1. **Перезапустите Cursor** - это важно для загрузки MCP сервера
2. Откройте проект `MCPWeather` в Cursor
3. MCP сервер должен автоматически подключиться

**Проверка подключения:**
- В Cursor откройте панель MCP серверов
- Должен отображаться сервер `weather` с инструментами:
  - `get_current_weather`
  - `get_weather_forecast`
  - `search_location`

**Использование:**
```
@weather get_current_weather location="Москва"
@weather get_weather_forecast days=5 location="Санкт-Петербург"
@weather search_location city_name="Новосибирск"
```

### Для Cursor IDE (Глобально - все проекты)

Если хотите использовать погоду во всех проектах:

1. Откройте глобальный конфиг Cursor:
   - **macOS:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Добавьте сервер (замените путь на ваш):

```json
{
  "mcpServers": {
    "weather": {
      "command": "/Users/alexdevyatov/PycharmProjects/MCPWeather/venv/bin/python",
      "args": ["/Users/alexdevyatov/PycharmProjects/MCPWeather/server.py"],
      "cwd": "/Users/alexdevyatov/PycharmProjects/MCPWeather",
      "env": {
        "DEFAULT_LANG": "ru",
        "DEFAULT_LOCATION": "55.75396,37.620393",
        "CACHE_TTL": "600",
        "LOG_LEVEL": "INFO",
        "REQUEST_TIMEOUT": "10"
      }
    }
  }
}
```

3. Перезапустите Cursor

### Для Claude Desktop

1. Откройте конфиг Claude Desktop:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

   Или через приложение: **Claude → Settings… → Developer → Edit Config**

2. Добавьте сервер:

```json
{
  "mcpServers": {
    "weather": {
      "command": "/Users/alexdevyatov/PycharmProjects/MCPWeather/venv/bin/python",
      "args": ["/Users/alexdevyatov/PycharmProjects/MCPWeather/server.py"],
      "cwd": "/Users/alexdevyatov/PycharmProjects/MCPWeather",
      "env": {
        "DEFAULT_LANG": "ru",
        "DEFAULT_LOCATION": "55.75396,37.620393",
        "CACHE_TTL": "600",
        "LOG_LEVEL": "INFO",
        "REQUEST_TIMEOUT": "10"
      }
    }
  }
}
```

3. Полностью закройте и перезапустите Claude Desktop

4. В чате нажмите «+» → **Connectors** и убедитесь, что отображается **weather**

## 📋 Доступные инструменты

| Инструмент | Описание | Пример |
|------------|----------|--------|
| `get_current_weather` | Текущая погода | `@weather get_current_weather location="Москва"` |
| `get_weather_forecast` | Прогноз на 1-7 дней | `@weather get_weather_forecast days=5 location="СПб"` |
| `search_location` | Поиск координат города | `@weather search_location city_name="Новосибирск"` |

## 🔍 Проверка работы

**Ручной тест сервера:**
```bash
cd /Users/alexdevyatov/PycharmProjects/MCPWeather
source venv/bin/activate
python server.py
```

Сервер должен запуститься и ждать ввода через stdio. Нажмите Ctrl+C для остановки.

**Проверка в Cursor:**
- Откройте панель MCP серверов
- Должен быть виден сервер `weather`
- Попробуйте использовать: `@weather get_current_weather location="Москва"`

## ⚙️ Настройки (опционально)

Создайте файл `.env` для кастомизации:

```bash
cp .env.example .env
```

Доступные параметры:
- `DEFAULT_LANG` - язык ответа (по умолчанию `ru`)
- `DEFAULT_LOCATION` - местоположение по умолчанию
- `CACHE_TTL` - время жизни кэша в секундах (по умолчанию 600)
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR)
- `REQUEST_TIMEOUT` - таймаут запросов в секундах

**Примечание:** Open-Meteo API не требует API ключа - полностью бесплатный!

## 🐛 Решение проблем

### Сервер не появляется в Cursor
1. Убедитесь, что Cursor перезапущен
2. Проверьте пути в `.cursor/mcp.json` (должны быть относительными с `${workspaceFolder}`)
3. Проверьте логи MCP в панели Cursor

### Ошибки при запуске
1. Проверьте версию Python: `venv/bin/python --version` (должна быть 3.11+)
2. Убедитесь, что зависимости установлены: `pip list | grep mcp`
3. Проверьте, что виртуальное окружение активировано

### Ошибки API
- Open-Meteo не требует API ключа
- Проверьте интернет-соединение
- Лимит: 10,000 запросов в день (бесплатно)

## 📚 Дополнительная документация

- [README.md](README.md) - полная документация
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт
- [AGENT_SETUP.md](AGENT_SETUP.md) - настройка для разных агентов
- [SETUP.md](SETUP.md) - настройка окружения

---

**Готово! MCP Weather сервер интегрирован и готов к использованию! 🌤️**
