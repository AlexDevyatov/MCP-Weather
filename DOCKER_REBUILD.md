# 🔄 Пересборка Docker образа после изменений кода

## Проблема

Если вы получаете ошибки валидации после изменения кода, Docker контейнер использует старый образ. Нужно пересобрать образ.

## Решение

### Вариант 1: Пересборка через docker compose (рекомендуется)

**Новая версия Docker (docker compose):**
```bash
# Остановите контейнер
docker compose down

# Пересоберите образ с обновленным кодом
docker compose build --no-cache

# Запустите заново
docker compose up -d

# Проверьте логи
docker compose logs -f
```

**Старая версия (docker-compose):**
```bash
# Остановите контейнер
docker-compose down

# Пересоберите образ с обновленным кодом
docker-compose build --no-cache

# Запустите заново
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

### Вариант 2: Пересборка через docker

```bash
# Остановите контейнер
docker stop mcp-weather
docker rm mcp-weather

# Пересоберите образ
docker build -t mcp-weather --no-cache .

# Запустите контейнер
docker run -d --name mcp-weather --restart unless-stopped \
  -p 9001:9001 \
  -e SERVER_PORT=9001 \
  -e SERVER_HOST=0.0.0.0 \
  mcp-weather

# Проверьте логи
docker logs -f mcp-weather
```

### Вариант 3: Быстрая пересборка

**Новая версия Docker:**
```bash
# Пересоберите и перезапустите одной командой
docker compose up -d --build
```

**Старая версия:**
```bash
# Пересоберите и перезапустите одной командой
docker-compose up -d --build
```

## Проверка

После пересборки проверьте, что изменения применены:

```bash
# Health check
curl http://localhost:9001/health

# Запустите тесты
python test_mcp.py --port 9001
```

## Примечание

Флаг `--no-cache` гарантирует полную пересборку без использования кэша. Это важно, если вы изменили код Python файлов.
