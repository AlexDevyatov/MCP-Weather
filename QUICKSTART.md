# 🚀 Быстрый старт

## Шаг 1: Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
# На macOS/Linux:
source venv/bin/activate

# На Windows:
# venv\Scripts\activate
```

## Шаг 2: Установка зависимостей

```bash
# Убедитесь, что виртуальное окружение активировано
pip install -r requirements.txt
```

## Шаг 3: Настройка (опционально)

**Отличная новость:** Open-Meteo API не требует API ключа! Сервер готов к использованию сразу после установки зависимостей.

Если вы хотите изменить настройки по умолчанию, создайте файл `.env`:
```bash
cp .env.example .env
```

Затем отредактируйте `.env` при необходимости (все параметры опциональны).

## Шаг 4: Интеграция с Cursor

Файл `.cursor/mcp.json` уже настроен. Просто:

1. Убедитесь, что виртуальное окружение создано и зависимости установлены
2. Перезапустите Cursor
3. MCP-сервер должен автоматически подключиться

**Примечание:** API ключ не требуется!

**Подключение к другим AI-агентам** (Claude Desktop, глобальный Cursor и т.д.) — см. **[AGENT_SETUP.md](AGENT_SETUP.md)**.

## Шаг 5: Использование

В Cursor вы можете использовать:

```
@weather get_current_weather location="Москва"
@weather get_weather_forecast days=5 location="Санкт-Петербург"
@weather search_location city_name="Новосибирск"
```

Или через промпты:

```
@weather current_weather location="Москва"
@weather weather_forecast location="Санкт-Петербург" days=3
@weather weather_summary location="Москва"
```

## 🐛 Решение проблем

### Ошибки API
- Open-Meteo не требует API ключа, поэтому эта ошибка не должна возникать
- Если возникают проблемы с API, проверьте интернет-соединение
- Open-Meteo имеет лимит 10,000 запросов в день для бесплатного использования

### Ошибка "Requires-Python >=3.10"
- MCP SDK требует Python 3.10 или выше
- Проверьте версию: `python3 --version`
- Если у вас Python 3.9 или ниже, обновите Python до версии 3.10+
- **Подробные инструкции по обновлению Python см. в [SETUP.md](SETUP.md)**
- Пересоздайте виртуальное окружение с новой версией: `python3.10 -m venv venv` или `python3.11 -m venv venv`

### Сервер не запускается
- Убедитесь, что виртуальное окружение создано и активировано
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Убедитесь, что используется Python 3.10+
- Проверьте, что в `.cursor/mcp.json` указан правильный путь к Python из venv

### Проблемы с геокодингом
- Если поиск города не работает, попробуйте использовать координаты напрямую
- Open-Meteo Geocoding API может не найти некоторые небольшие города
- В таком случае используйте формат: `location="55.75396,37.620393"`

## 📚 Подробная документация

См. [README.md](README.md) для полной документации.
